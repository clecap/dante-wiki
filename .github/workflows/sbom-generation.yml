name: Generate SBOM for Dockerfiles

on:
  push:
    branches:
      - master
  schedule:
    - cron: '0 0 * * *'  # Runs every day at midnight      

jobs:
  generate-sbom:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7

    - name: Set up Python environment
      uses: actions/setup-python@b64ffcaf5b410884ad320a9cfac8866006a109aa # v4.8.0
      with:
        python-version: '3.x'

    - name: Install syft
      run: |
        pip install syft

    - name: Generate SBOM for Dockerfile
      run: |
        mkdir -p .github/results
        syft dir:images/lap          -o json        > .github/results/sbom-lap.json
        syft dir:images/dante-mysql  -o json        > .github/results/sbom-dante-mysql.json
        syft dir:images/tex/         -o json        > .github/results/sbom-tex.json
        syft dir:images/lap          -o spdx        > .github/results/sbom-lap.spdx
        syft dir:images/dante-mysql  -o spdx        > .github/results/sbom-dante-mysql.spdx
        syft dir:images/tex/         -o spdx        > .github/results/sbom-tex.spdx
        syft dir:images/lap          -o cyclonedx   > .github/results/sbom-lap.xml
        syft dir:images/dante-mysql  -o cyclonedx   > .github/results/sbom-dante-mysql.xml
        syft dir:images/tex/         -o cyclonedx   > .github/results/sbom-tex.xml


#  --output syft-json   produced error message
#  --output json        produced error message


    - name: Commit SBOM files
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add .github/results/sbom-*.json
        git add .github/results/sbom-*.spdx
        git add .github/results/sbom-*.xml
        git commit -m "Add auto-generated SBOM files"
        git push origin ${{ github.ref }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
